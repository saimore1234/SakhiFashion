Place your own assets here.

banner.mp4   -> optional default hero video (you can also upload via /admin)
favicon.ico  -> site favicon

The hero video is managed from the Admin Panel (/admin > Banner Video).
Uploaded files preview from browser memory; for a permanent banner, host
the video on a CDN/storage and paste the URL in the admin "Use URL" field.
